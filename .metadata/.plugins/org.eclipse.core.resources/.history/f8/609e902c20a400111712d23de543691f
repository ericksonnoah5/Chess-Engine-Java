package chess_game;

import java.util.*;


import chess_game.*;
import pieces.*;

public class main {

	static Scanner scan;
	static chess_game chessgame;
	
	public static void main (String args [] ) {
		
		chessgame = new chess_game();
		scan = new Scanner(System.in);
		String piece;
		String where;
		int [] piececoords;
		int [] wherecoords;
		boolean whitesturn=true;
		boolean running=true;

		
		while(running) {
			
			System.out.println("Commands q(uit) h(elp)");
			System.out.println("");
			if(whitesturn) {
				System.out.println("Whites turn");
			}
			
			else {
				System.out.println("Blacks turn");
			}

			System.out.println("");

			
			chessgame.board.printBoard();
			System.out.println("");

	        System.out.print("Piece: ");
	        System.out.print("");

	        piece = scan.nextLine();
	        
	        
	        validate(piece);
	        piececoords=decode(piece);
	        
	        
			System.out.println("");
			
	        System.out.print("Where: ");

	        where = scan.nextLine();
	        
	        validate(where);
	        wherecoords=decode(where);

			System.out.println("");

			
			
			movepiece(piececoords,wherecoords);
			
			
			
			
			
			
			
			
	        chess_game.clearTerminal();
	       
	        
		}
	}
	
	
	
	
	
	
	
	
	private static void movepiece(int[] piececoords, int[] wherecoords) {

		piece temp = chessgame.board.squares[piececoords[0]][piececoords[1]].piece;//00
		
		
	}








	public static int[] decode(String piece) {
		int [] arr = new int [2];
		int x = 0;
		int y=0;
		
		String validNumbers="12345678";
		String validChars="abcdefgh";
		
		String first = piece.substring(0,1);
		String secound = piece.substring(1,2);
		
		for(int i=0;i<8;i++) {
			if(validChars.charAt(i)==first.charAt(0)) {
				x=i;
			}
		}
		for(int i=0;i<8;i++) {
			if(validNumbers.charAt(i)==secound.charAt(0)) {
				y=i;
			}
		}
		
		
		arr[0]=x;
		arr[1]=y;
		return arr;
	}
	
	public static void validate(String piece) {
		
		String validNumbers="12345678";
		String validChars="abcdefgh";
		
		
		if(piece==null || piece=="" || piece.length()!=2 ) {
			System.out.println("try again example(e4)");

			
			System.out.print("Piece: ");
	        piece = scan.nextLine();
	        validate(piece);
	        return;
		}
		
		String first = piece.substring(0,1);
		String secound = piece.substring(1,2);
		
		if(validChars.contains(first)) {
			
		}
		else {
			System.out.println("try again example(e4)");
			System.out.print("Piece: ");
	        piece = scan.nextLine();
	        validate(piece);
	        return;
		}
		
		if(validNumbers.contains(secound)) {
			
		}
		else {
			System.out.println("try again example(e4)");
			System.out.print("Piece: ");
	        piece = scan.nextLine();
	        validate(piece);
	        return;
		}
		
		
		
		
		
		
		
	}
}
